/**
 * Barrel for the renderer hook cache (sidecar renderer-normalization).
 */

export { hookClientCacheManager } from './hookClientCacheManager';
export type { HookDefinition } from './hookClientCacheManager';
export { useHooks } from './useHooks';
